<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(Program).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="typeof(MyLayout)" Context="context">
                <Authorizing>
                    @*<RedirectToSurveyForm />*@
                    Working.....
                </Authorizing>
                <NotAuthorized>
                    @{
                        @*if (context.User.Claims.Count() > 0 && !context.User.Claims.Any(x => x.Type.Equals("FilledSurveyForm")))
                            {
                                <RedirectToSurveyForm />
                            }
                            else
                            {
                            }*@
                        <RedirectToLogin />
                    }
                </NotAuthorized>
            </AuthorizeRouteView>
        </Found>
        <NotFound>
            <LayoutView Layout="@typeof(NotFound)">
                @*<p>Sorry, there's nothing at this address.</p>*@
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>